{% set name = "pgrouting" %}
{% set version = "2.6.3" %}
{% set sha256 = "7ebef19dc698d4e85b85274f6949e77b26fe5a2b79335589bc3fbdfca977eb0f" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/pgrouting/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script:
    - mkdir build
    - cd build
    - cmake ..
    - make
    - make install
  skip: True  # [win]


requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - cgal
    - cmake
    - gmp
    - perl
    - pkg-config
    - postgresql
    - postgis
  run:
    - {{ pin_compatible('cgal', exact=True) }}
    - postgis
    - postgresql

test:
  commands:
    - bash {{ RECIPE_DIR }}/run_test.sh  # [not win]

about:
  home: http://github.com/pgrouting/pgrouting
  license: GPL-2.0
  license_family: GPL
  license_file: LICENSE
  summary: 'pgRouting extends PostGIS/PostgreSQL geospatial database to provide geospatial routing and other network analysis functionality.'

  doc_url: https://docs.pgrouting.org
  dev_url: https://github.com/pgrouting/pgrouting

extra:
  recipe-maintainers:
    - sodre
